Flowlytics version 2.4 removes the deprecated `/v1/export` endpoints. These endpoints are no longer operational and will return an error if called. All customers must migrate their integrations and data pipelines to the `/v2/export` endpoints to ensure continuous access to export functionality. Please review the version 2.4 migration guide for updated payload structures and authentication requirements.
Flowlytics v2.0 introduces a complete rewrite of the query engine, replacing the v1 row store with a new columnar query engine. This architectural shift significantly improves aggregation performance and reduces memory consumption during large analytical workloads. Existing queries are fully compatible with v2.0, but customers should expect noticeably faster response times for all historical data retrieval and custom dashboard rendering.
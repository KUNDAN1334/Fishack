In Flowlytics v2.4, the webhook retry limit has been increased to 5 attempts. Previously, failed webhook deliveries were retried up to 3 times before being marked as permanently failed. This update includes a longer backoff tail between subsequent attempts to improve reliability during temporary network issues or downstream service outages.
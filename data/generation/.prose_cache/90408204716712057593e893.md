Flowlytics v2.4 addresses a race condition in the delivery worker that previously caused occasional duplicate webhook deliveries. This fix ensures each event payload is processed and sent exactly once by implementing a distributed locking mechanism across concurrent worker threads, resolving ingestion redundancy for downstream subscriber endpoints.
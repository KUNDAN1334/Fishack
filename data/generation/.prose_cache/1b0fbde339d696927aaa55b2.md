Flowlytics v2.3 introduces API key scopes, allowing administrators to restrict access to specific resources when generating new credentials. Instead of granting blanket access across your entire account, you can now limit individual keys to exact operational domains such as analytics querying or billing management. This limits potential exposure if a credential is compromised. Existing API keys remain unaffected and retain full access until manually updated or replaced in your settings dashboard.
# Secrets — never commit real keys; .env.example is the committed template
.env

__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
dist/
build/

# Frontend (Phase 6)
node_modules/
.next/

# Local model caches (sentence-transformers downloads, Phase 1+)
.model_cache/

# Eval run artifacts are timestamped local outputs; committed baselines live
# in data/golden/baselines/ explicitly
fishnet_reports/

# NOT ignored, deliberately (ADR-008):
#   data/raw/                        the generated corpus — committed so the
#                                    golden set has stable ground truth
#   data/generation/.prose_cache/    LLM prose cache — committed so a fresh
#                                    clone reproduces the corpus with no API key

# Cross-session working notes — not project content
HANDOFF.md

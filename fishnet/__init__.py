"""fishnet — Fishly's eval harness (it catches bad answers).

Arrives in Phase 4: golden dataset, retrieval metrics (recall@K, MRR),
LLM-as-judge generation metrics, scorecards, CI regression gate.
Runnable via: python -m fishnet.run
"""
